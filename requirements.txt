# Core data & ML
numpy>=2.2.1
pandas>=2.2.3
scikit-learn>=1.6.1
joblib>=1.4.2

# Excel I/O
openpyxl>=3.1.5

# Config & serialization
PyYAML>=6.0.2
jsonschema   # if you ever validate JSON schemas (optional)

# DVC client (only if you want to run `dvc pull` inside your container)
dvc>=2.50.0

# (Optional) CLI utils
click>=8.1.8    # if you add any CLI entrypoints later
